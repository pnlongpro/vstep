export enum PracticeMode {
  PRACTICE = 'practice',
  MOCK_TEST = 'mock_test',
  REVIEW = 'review',
}

export enum SessionStatus {
  IN_PROGRESS = 'in_progress',
  PAUSED = 'paused',
  COMPLETED = 'completed',
  ABANDONED = 'abandoned',
  EXPIRED = 'expired',
}
